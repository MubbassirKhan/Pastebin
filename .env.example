# Environment variables for Upstash Redis / Vercel KV
# Copy this file to .env.local and fill in your values

# Upstash Redis credentials (from Vercel integration or Upstash dashboard)
# Vercel KV uses these same variable names
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Alternative: Direct Upstash Redis credentials
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Optional: Enable test mode for deterministic time handling
# TEST_MODE=1
